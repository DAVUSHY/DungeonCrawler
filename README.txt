## Running the server
chmod +x run_server.sh
./run_server.sh

## Installing the client
adb install android-debug.apk
Or manually transfer android-debug.apk to an Android device and install it.

## Requirements
- Java 11+
- Android device running Android 8.0+